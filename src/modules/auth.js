
const state = {
	isLoading: false,
	user: null,
	errors: null,
	isLoggedIn: null,
}

const getters = {
	
}

const mutations = {
	
}

const actions = {
	
}

export default {
	state,
	mutations,
	actions,
	getters,
}
